namespace FG {
    /// <summary>
    /// What kind of message type, if it's a success-message or error-message
    /// </summary>
    public enum MessageType {
        Success = 0,
        Error = 1,
    }
}